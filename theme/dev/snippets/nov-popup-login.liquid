{%- comment -%}
  The purchased ONE theme's layout/theme.liquid renders this snippet, but the
  theme does not ship it. That printed a visible Liquid error on every page
  served by that layout:

    Liquid error (layout/theme line 146): Could not find asset
    snippets/nov-popup-login.liquid

  This deliberately-empty stub removes the error without editing the vendor's
  layout. If a login popup is ever wanted, build it here.
{%- endcomment -%}
